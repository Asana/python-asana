# This file is automatically generated by generate.py using api.json

class Tasks:
    def __init__(self, dispatcher=None):
        self.dispatcher = dispatcher

    def add_followers(self, task_id, params={}):
        path = '/tasks/%d/addFollowers' % (task_id)
        return self.dispatcher.post(path, params)

    def tags(self, task_id, params={}):
        path = '/tasks/%d/tags' % (task_id)
        return self.dispatcher.get(path, params)

    def add_project(self, task_id, params={}):
        path = '/tasks/%d/addProject' % (task_id)
        return self.dispatcher.post(path, params)

    def create(self, params={}):
        return self.dispatcher.post('/tasks', params)

    def remove_tag(self, task_id, params={}):
        path = '/tasks/%d/removeTag' % (task_id)
        return self.dispatcher.post(path, params)

    def update(self, task_id, params={}):
        path = '/tasks/%d' % (task_id)
        return self.dispatcher.put(path, params)

    def find_by_project(self, project_id, params={}):
        path = '/workspaces/%d/tasks' % (project_id)
        return self.dispatcher.get(path, params)

    def find_by_id(self, task_id, params={}):
        path = '/tasks/%d' % (task_id)
        return self.dispatcher.get(path, params)

    def subtasks(self, task_id, params={}):
        path = '/tasks/%d/subtasks' % (task_id)
        return self.dispatcher.get(path, params)

    def add_subtask(self, task_id, params={}):
        path = '/tasks/%d/subtasks' % (task_id)
        return self.dispatcher.post(path, params)

    def add_tag(self, task_id, params={}):
        path = '/tasks/%d/addTag' % (task_id)
        return self.dispatcher.post(path, params)

    def remove_followers(self, task_id, params={}):
        path = '/tasks/%d/removeFollowers' % (task_id)
        return self.dispatcher.post(path, params)

    def find_all(self, params={}):
        return self.dispatcher.get('/tasks', params)

    def create_in_workspace(self, workspace_id, params={}):
        path = '/workspaces/%d/tasks' % (workspace_id)
        return self.dispatcher.post(path, params)

    def remove_project(self, task_id, params={}):
        path = '/tasks/%d/removeProject' % (task_id)
        return self.dispatcher.post(path, params)

    def set_parent(self, task_id, params={}):
        path = '/tasks/%d/setParent' % (task_id)
        return self.dispatcher.post(path, params)

    def projects(self, task_id, params={}):
        path = '/tasks/%d/projects' % (task_id)
        return self.dispatcher.get(path, params)

    def delete(self, task_id, params={}):
        path = '/tasks/%d' % (task_id)
        return self.dispatcher.delete(path, params)
